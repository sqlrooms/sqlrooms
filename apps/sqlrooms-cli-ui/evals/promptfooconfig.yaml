# yaml-language-server: $schema=https://promptfoo.dev/config-schema.json
description: SQLRooms document charts/maps behavioral canary

prompts:
  - label: pinned-scenario-turn
    raw: '{{prompt}}'

providers:
  - id: file://./dist/promptfooProvider.mjs
    label: sqlrooms-embedded-openrouter

tests:
  - description: document.create-chart-map@2
    vars:
      scenarioId: document.create-chart-map
      prompt: >-
        Create exactly one new Document named Event analysis. In it, add a quantitative chart of metric by category and a geographic point map using latitude and longitude from analytics.events. Do not use archive.events and do not create a dashboard or a second document. Summarize exactly what you created and which table you used.
    metadata:
      scenarioVersion: 2
      profile: document-charts-maps@1
    assert:
      - type: javascript
        metric: sqlrooms-checks
        value: context.metadata.sqlroomsAssertion
  - description: document.mutate-chart-map@2
    vars:
      scenarioId: document.mutate-chart-map
      prompt: >-
        In the current Document, update the existing chart in place so its title is "Metric by category", then add one short paragraph saying "Source: analytics.events". Preserve the existing map and heading exactly. Do not create another chart, map, document, or dashboard. Report only changes that actually succeeded.
    metadata:
      scenarioVersion: 2
      profile: document-charts-maps@1
    assert:
      - type: javascript
        metric: sqlrooms-checks
        value: context.metadata.sqlroomsAssertion

evaluateOptions:
  repeat: 3
  maxConcurrency: 1
  cache: false
  timeoutMs: 240000

metadata:
  profile: document-charts-maps@1
  modelSource: SQLROOMS_EVAL_MODEL (resolved model recorded in sqlroomsEvidence)
  modelProvider: openrouter
  modelSettings: temperature=0,maxSteps=24
  suiteVersion: 2

writeLatestResults: true
