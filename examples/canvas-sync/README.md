# Canvas Example

A demonstrating `@sqlrooms/canvas` with ReactFlow that also shows CRDT sync integration. Click + to add nodes and drag to reposition.

Server start instructions:

- From project root: `cd python/sqlrooms-server`
- Run: `pnpm run sync`
