# `@sqlrooms/color-scales`

Shared color-scale config, validation, legend models, and React legend components
for SQLRooms visualizations.

This package is intentionally generic: it does not know about DeckGL layers,
Arrow tables, or Mosaic/VGPlot. Adapter code for those runtimes lives in the
packages that consume this one.
