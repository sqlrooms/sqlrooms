/**
 * Re-exported from `@sqlrooms/documents` so the searchable table selector
 * stays shared across document blocks (charts, maps, …). Kept here for
 * backward compatibility with existing `@sqlrooms/mosaic` imports.
 */
export {
  DataTableSelector,
  DataTableSelectorEmptyState,
  type DataTableSelectorProps,
} from '@sqlrooms/documents';
