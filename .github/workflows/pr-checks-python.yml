name: Python PR Checks

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'python/**'
      - 'apps/sqlrooms-cli-ui/**'
      - '.github/workflows/pr-checks-python.yml'
  pull_request:
    types: [opened, edited, synchronize, reopened]
    paths:
      - 'python/**'
      - 'apps/sqlrooms-cli-ui/**'
      - '.github/workflows/pr-checks-python.yml'

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request' || github.event.action != 'edited' || github.event.pull_request.user.login == 'renovate[bot]'

    defaults:
      run:
        working-directory: python

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          version: '0.11.23'
          enable-cache: true
          cache-dependency-glob: |
            python/uv.lock
            python/pyproject.toml
            python/*/pyproject.toml

      - uses: pnpm/action-setup@f40ffcd9367d9f12939873eb1018b921a783ffaa
        name: Install pnpm
        with:
          run_install: false

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: '24'
          cache: 'pnpm'

      - name: Install JS dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts
        working-directory: .

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: uv sync --all-packages --all-extras

      - name: Lint with ruff
        run: uv run ruff check .

      - name: Prepare CLI UI and Claude plugin
        run: pnpm --filter sqlrooms-python build:ui
        working-directory: .

      - name: Build packages
        run: uv build --all

      - name: Verify sqlrooms wheel
        run: python sqlrooms/scripts/verify_wheel.py dist/sqlrooms-*.whl

      - name: Verify isolated runtime and sdist installs
        run: python sqlrooms/scripts/verify_distribution.py dist/sqlrooms-*.whl dist/sqlrooms-*.tar.gz

      - name: Install Roomie for paired checkout tests
        # Roomie requires an unreleased sqlrooms version and is intentionally
        # outside the uv workspace. Test it against the installed checkout.
        run: uv pip install --python .venv/bin/python --no-deps --editable roomie

      - name: Run tests
        run: uv run --no-sync pytest
