{
  "**/*.{ts,tsx,js}": [
    "turbo lint --filter=@sqlrooms/* --",
    "prettier --write"
  ],
  "**/*.md": ["prettier --write"],
  "**/package.json": ["sort-package-json"],
  "python/**/*.py": [
    "uv run --project python ruff check --fix",
    "uv run --project python ruff format"
  ]
}
