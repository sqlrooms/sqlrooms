{
  "name": "sqlrooms-duckdb-server",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "dev": "uv run --group dev watchmedo auto-restart --pattern '*.py' --recursive --signal SIGTERM -- uv run python -m pkg --db-path /tmp/sqlrooms-duckdb-server-dev.db --port 4000",
    "test": "uv run pytest",
    "prerelease": "uv run pytest && uv run ruff check && uv run ruff format --check",
    "release": "uv build && uvx twine upload --skip-existing ./dist/sqlrooms_duckdb_server*"
  }
}
